// Package entity defines the semantic storage entity model — the layer
// between raw filesystem paths and cleanup findings — covering kinds such
// as Application, Cache, Archive, ArtifactFamily, Database, Container,
// VM, Model, and Project.
package entity
