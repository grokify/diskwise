// Package service defines the Service interface — the single API surface
// shared by the CLI and MCP adapters — along with its command and query
// request/response types, which double as the JSON contract for both
// adapters.
package service
