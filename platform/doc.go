// Package platform provides OS-specific filesystem primitives —
// allocated size, volume capacity, and standard directories — behind a
// single interface, keeping macOS-specific syscalls out of the rest of
// the core.
package platform
