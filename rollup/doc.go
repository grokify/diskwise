// Package rollup aggregates findings into the smallest set of paths such
// that every emitted directory is entirely covered by a finding — the
// directly-actionable unit for manual deletion.
package rollup
